#tuples cant be changed once set
coordinates = (109,20)

#still follows laws of arrays in terms of accessing its elements
print(coordinates[0])

#list of tuples that can be used for data that will not change

coords = [ (22,50), (30,100), (100,20)]

print(coords[0][1])

